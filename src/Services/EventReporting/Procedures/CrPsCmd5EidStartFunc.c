/**
 * @file CrPsCmd5EidStartFunc.c
 *
 * @author FW Profile code generator version 5.01
 * @date Created on: Sep 6 2017 17:17:44
 */

/** CrPsCmd5EidStart function definitions */
#include "CrPsCmd5EidStartCreate.h"

/** FW Profile function definitions */
#include "FwPrConstants.h"
#include "FwPrDCreate.h"
#include "FwPrConfig.h"
#include "FwPrCore.h"
#include "CrPsDebug.h"
#include "CrFwConstants.h"

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/* ----------------------------------------------------------------------------------------------------------------- */


/** Action for node N1. */
void CrPsCmd5EidStartN1(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartN1. \n");
}

/** Action for node N2. */
void CrPsCmd5EidStartN2(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartN2. \n");
}

/** Action for node N3. */
void CrPsCmd5EidStartN3(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartN3. \n");
}

/** Action for node N4. */
void CrPsCmd5EidStartN4(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartN4. \n");
}

/** Action for node N7. */
void CrPsCmd5EidStartN7(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartN7. \n");
}

/** Action for node N8. */
void CrPsCmd5EidStartN8(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartN8. \n");
}

/** Guard on the Control Flow from DECISION1 to N2. */
FwPrBool_t CrPsCmd5EidStartG1(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartG1. \n");
	return 0;
}

/** Guard on the Control Flow from DECISION3 to DECISION4. */
FwPrBool_t CrPsCmd5EidStartG2(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartG2. \n");
	return 0;
}

/** Guard on the Control Flow from DECISION4 to N8. */
FwPrBool_t CrPsCmd5EidStartG3(FwPrDesc_t prDesc)
{
	CRFW_UNUSED(prDesc);
	DEBUGP_5("CrPsCmd5EidStartG3. \n");
	return 0;
}

/* ----------------------------------------------------------------------------------------------------------------- */

